import {useState} from 'react';

const Board = () => {
    const [width, setWidth] = useState(5);  // Number of columns
    const [height, setHeight] = useState(5); // Number of rows

    return (
        <div>
           
        </div>
    );
};

export default Board;